apiVersion: v1
kind: Service
metadata:
  name: sps-service
spec: 
  type: NodePort
  selector:
    app: sps
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30100